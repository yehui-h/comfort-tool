import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
} from "./button.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
